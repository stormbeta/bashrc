# Reuse Exisiting Connections if available
ControlMaster 	auto
ControlPath 	~/.ssh/tmp/%h_%p_%r

# Other Global Settings
ForwardAgent	yes
ExitOnForwardFailure yes

# Set defaults for all hosts
Host *
	User dborg

# Setup a shortcut to blacksheep (my box) @ the office
Host blacksheep.1900proxy
	HostName blacksheep.e.ecovate.com
  ProxyCommand ssh -q 1900.readytalk.com nc -q0 blacksheep.e.ecovate.com 22
